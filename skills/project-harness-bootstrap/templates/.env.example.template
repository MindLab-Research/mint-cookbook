# Shared credential template for the repo.
# Copy this file to the local `.env` beside the entrypoint you plan to run.
# Examples:
# - from `experiments/<name>/`: `cp ../../.env.example .env`
# - from `tests/`: `cp ../.env.example .env`
# Use these MinT runtime credentials for maintained experiments and new scaffold-derived experiments:
MINT_BASE_URL=https://mint-cn.macaron.xin
MINT_API_KEY=sk-your-api-key-here
